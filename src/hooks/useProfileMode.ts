// Re-export from the centralized context
// This maintains backwards compatibility with existing imports
export { useProfileMode, ProfileModeProvider } from '@/contexts/ProfileModeContext';
export type { ProfileModeType, ProfileModeState } from '@/contexts/ProfileModeContext';
